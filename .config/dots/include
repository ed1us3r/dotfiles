# User specific environment and startup programs
if [ -f ~/.config/dots/powerline ]; then
    . ~/.config/dots/powerline
fi

# Enable custom scripts
if [ -f ~/.config/dots/nvim ]; then
    . ~/.config/dots/nvim
fi
